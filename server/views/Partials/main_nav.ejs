<!--NavBar-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid"> <!-- keeps navbar content alligned -->
      <a class="navbar-brand" href="/">Shoe Tracker</a>
      <!-- toggle button for mobile view -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <% if(title=="Home") { %>
              <a class="nav-link active" href="/home">
            <% } else {%>
              <a class="nav-link" href="/home">
            <% } %>
            <i class="fa-solid fa-house-chimney-user"></i> Home</a>
          </li>
          <li class="nav-item">
            <% if(title=="Collection") { %>
              <a class="nav-link active" href="/collection">
            <% } else {%>
              <a class="nav-link" href="/collection">
            <% } %>    
              <i class="fa-solid fa-shoe-prints"></i> Collection</a>
          </li>
          <li class="nav-item">
            <% if(title=="Shoes") { %>
              <a class="nav-link active" href="/shoeslist">
            <% } else {%>
              <a class="nav-link" href="/shoeslist">
            <% } %>  
            <i class="fa-solid fa-truck-fast"></i> Shoes</a>
          </li>
          <li class="nav-item"></li>
          <% if(title=="Contact Us") { %>
            <a class="nav-link active" href="/contactus">
          <% } else {%>  
            <a class="nav-link" href="/contactus">
          <% } %> 
          <i class="fa-solid fa-paper-plane"></i> ContactUs</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>